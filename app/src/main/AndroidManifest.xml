<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_app_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_app"
        android:supportsRtl="true"
        android:theme="@style/Theme.GroceryBudgetPlanner"
        tools:targetApi="31">
        <activity
            android:name=".controller.checkout_page"
            android:exported="false" />
        <activity
            android:name=".controller.budget_page"
            android:exported="false" />
        <activity
            android:name=".controller.home_page"
            android:exported="false" />
        <activity
            android:name=".controller.splash_screen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true" />
        <activity
            android:name=".controller.admin_page"
            android:exported="false" />
        <activity
            android:name=".controller.registration_page"
            android:exported="false" />
        <activity
            android:name=".controller.login_page"
            android:exported="false" />
    </application>

</manifest>